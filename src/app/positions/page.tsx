import { PositionDescriptions } from "@/components/public/position-descriptions";

export const metadata = {
  title: "Position Descriptions · St. Clement Strawberry Festival",
};

export default function PositionsPage() {
  return <PositionDescriptions />;
}
